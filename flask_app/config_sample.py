# Dictionary to determine which security settings should be included in templates (default: all supported settings)
tracked_settings = {
    "mx_l3_outbound_firewall": True,
    "mx_l7_firewall": True,
    "mx_content_rules": True
}
